# Laravel API origin (no trailing slash). Leave empty to use in-memory mock data.
VITE_API_BASE_URL=http://localhost:8000

# Show sign out confirmation dialog (true/false). Default: false (instant logout)
VITE_SHOW_SIGNOUT_DIALOG=false
